# -*- coding: utf-8 -*-

import numpy as np

class Maxpooling:
    def __init__(self, kernel, stride, pad):
        self.kernel = kernel
        self.stride = stride
        self.pad = pad
        return
        
    def forward(self, x):
        return
        
    def backward(self, d):
        return
